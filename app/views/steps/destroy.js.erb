$(".modal-backdrop").remove()
$("body").removeClass("modal-open")

$(".nextsteps-userview").replaceWith("<%= j render 'my_process', application: @application %>")

$.NotificationApp.send("Validated step! You're moving forward in this process!", 'top-right', 'Background color', 'success');