<!-- NAVBAR PAS TERMINEE-->

<% if signed_in? %>
<nav class="navbar navbar-expand-lg navbar-light bg-white">

<div class="container">

      <div class="col" > 

        <div class="row">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "My Career Manager", applications_path, class:"navbar-brand" %>
      </li>
      <li class="nav-item lg-link">
        <%= link_to "Create an application", new_application_path, class:"nav-link d-flex" %>
      </li>
      <li class="nav-item lg-link">
        <%= link_to "Admin Dashboard", admin_root_path, class:"nav-link d-flex" %>
      </li>
        </ul>
        </div>
      </div>

      <div id="sm-menu" class="row justify-content-end">
        <div class="collapse navbar-collapse" id="navbarSupportedContent" >

          <ul class="navbar-nav mr-auto togglelist">
             <li class="nav-item sm-link">
                <%= link_to "Create an application", new_application_path, class:"nav-link d-flex" %>
            </li>
              <li class="nav-item sm-link">
                <%  if current_user.is_admin == true %> <%= link_to "Admin Dashboard", admin_root_path, class:"nav-link d-flex" %> <%end%>
            </li>
            
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle d-flex" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
               <% if user_signed_in? %> <%=current_user.email%> <%end%> <i class="material-icons">account_circle</i>
              </a> 

              <% if user_signed_in? %>

                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <%= link_to "My profile", user_path(current_user), class:"dropdown-item" %>
                  <%= link_to "Log Out", destroy_user_session_path, method: :delete, class:"dropdown-item" %>
                </div>  

 
              <% else %>

                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <%= link_to "Login", user_session_path, class:"dropdown-item" %>
                  <%= link_to "Sign-up", new_user_registration_path, class:"dropdown-item" %>
                </div>    

              <% end %>
            </li>

          </ul>

        </div>
      </div>

</div>  <!-- container end -->

</nav>

<% end %>
